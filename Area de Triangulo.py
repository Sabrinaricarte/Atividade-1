base = float(input('Digite o número da base de seu triangulo: '))
altura = float(input('Digite o número da altura de seu triangulo: '))

area = (base * altura)/2

print('Está e a área do seu triangulo: {:.2f}'.format(area))